import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  wrapper: {
    width: '100%',
    paddingVertical: 24,
    paddingHorizontal: 16,
    alignItems: 'center',
    justifyContent: 'center',
  },
  icon: {
    // TODO: Adjust design tokens if you have them
    width: 56,
    height: 56,
    marginBottom: 12,
  },
})
